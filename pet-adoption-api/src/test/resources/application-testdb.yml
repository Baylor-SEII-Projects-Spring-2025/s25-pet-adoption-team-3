server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://docker-db-1:3307/pet_adoption_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true